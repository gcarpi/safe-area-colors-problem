import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'safe-area-colors-problem',
  webDir: 'dist',
  server: {
    androidScheme: 'http'
  }
};

export default config;
